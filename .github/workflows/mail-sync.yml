name: Correos automáticos desactivados

on:
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: swiftport-mail-sync
  cancel-in-progress: false

jobs:
  process-mail:
    name: Proceso detenido temporalmente
    runs-on: ubuntu-latest
    steps:
      - name: No leer correos
        run: |
          echo "La lectura automática de correos está desactivada temporalmente."
